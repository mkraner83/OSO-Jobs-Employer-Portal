The main boot file must:
- Confirm OSO Jobs Portal is active
- Load dependencies
- Boot the registration handler

---

# 📌 CODING STYLE REQUIREMENTS

Follow these WordPress conventions:

- Use strict checks:
  - `if ( ! defined('ABSPATH') ) exit;`
- Use master plugin constants:
  - `OSO_JOBS_PORTAL_DIR`
  - `OSO_Jobs_Portal::ROLE_EMPLOYER`
  - `OSO_Jobs_Portal::POST_TYPE_EMPLOYER`
- Use WP functions:
  - `wp_insert_user()`
  - `wp_new_user_notification()`
  - `wp_insert_post()`
  - `update_post_meta()`
- All logic related to employer registration must be contained inside  
  `class-oso-employer-registration.php`

---

# 📌 WHAT THE AI SHOULD DO NEXT

The agent may be asked to:

- Create new modules under `/includes`
- Improve employer workflow
- Build employer dashboards
- Add admin-side features
- Add front-end shortcodes
- Add AJAX endpoints
- Modify employer meta handling
- Add settings pages

### ❗ Do NOT:
- Modify any files in the master OSO Jobs Portal plugin
- Change CPT slugs
- Change existing roles
- Rewrite core logic of the master plugin

---

# 📌 MASTER PLUGIN DEFINITION NOTES (TO HELP AI)

The master plugin includes:

- Class `OSO_Jobs_Portal` (bootstrap)
- Utility class `OSO_Jobs_Utilities`
- CPT definitions
- WPForms handler class
- Admin menu class
- Settings handler
- Template loader
- Frontend shortcodes
- Jobseeker workflows
- Resume/photo upload handlers

The add-on should leverage these, not replace them.

---

# 📌 AGENT CONTEXT SUMMARY

You are building a **modular extension**, not editing the core.  
You must generate clean, modern, WordPress-standard code.  
All logic must fit inside the add-on plugin.  
All workflows must remain compatible with the master plugin.

Follow all rules above whenever writing code.